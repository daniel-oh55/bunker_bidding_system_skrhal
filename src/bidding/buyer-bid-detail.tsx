import { useState } from 'react';
import type { BiddingClient, BiddingResult } from './bidding-client';
import { FuelRows } from './bid-form';
import { isoToLocalInput, localInputToIso } from './datetime';
import { fuelGrades, type ActiveBuyer, type Bid, type BidAuditEvent, type BidTraderAccess, type Quote, type TraderOrganization } from './types';

type Detail = { access: BidTraderAccess[]; quotes: Quote[]; audit: BidAuditEvent[] };
type Row = { grade: typeof fuelGrades[number]; quantity: string };
const number = (value: number) => new Intl.NumberFormat(undefined, { maximumFractionDigits: 2 }).format(value);
const date = (value: string | null) => value ? new Date(value).toLocaleString() : 'No deadline';
const updateInput = (draft: { vessel: string; port: string; window: string; deadline: string; rows: Row[] }) => ({ vesselVoyage: draft.vessel, portName: draft.port, deliveryWindow: draft.window, deadlineAt: localInputToIso(draft.deadline), fuelGrades: draft.rows.map((row) => row.grade), quantities: draft.rows.map((row) => Number(row.quantity)) });

export function BuyerBidDetail({ bid, buyers, organizations, detail, pending, client, membershipId, mutate, refresh }: { bid: Bid; buyers: ActiveBuyer[]; organizations: TraderOrganization[]; detail: Detail | null; pending: boolean; client: BiddingClient; membershipId: string; mutate: (operation: () => Promise<BiddingResult<Bid>>) => Promise<boolean>; refresh: () => void }) {
  // The keyed parent recreates this complete local draft whenever ID or revision changes.
  const [draft, setDraft] = useState(() => ({ vessel: bid.vessel_voyage, port: bid.port_name, window: bid.delivery_window, deadline: isoToLocalInput(bid.deadline_at), rows: bid.fuel_items.map((item) => ({ grade: item.fuel_grade, quantity: String(item.quantity_mt) })) }));
  const [responsible, setResponsible] = useState(bid.responsible_buyer_user_id); const [scope, setScope] = useState(''); const [cancelConfirm, setCancelConfirm] = useState(false); const [awardConfirm, setAwardConfirm] = useState<string | null>(null);
  const commercialOpen = detail !== null && detail.quotes.length === 0 && bid.effective_status === 'open';
  const deadlineOpen = bid.effective_status === 'open';
  const validRows = draft.rows.length > 0 && draft.rows.every((row) => Number.isFinite(Number(row.quantity)) && Number(row.quantity) > 0);
  const saveBid = () => { if (deadlineOpen && validRows && (commercialOpen || (draft.vessel === bid.vessel_voyage && draft.port === bid.port_name && draft.window === bid.delivery_window && draft.rows.every((row, index) => row.grade === bid.fuel_items[index]?.fuel_grade && Number(row.quantity) === bid.fuel_items[index]?.quantity_mt)))) void mutate(() => client.updateBid(membershipId, bid.id, bid.revision, updateInput(draft))); };
  return <div><h2>{bid.vessel_voyage}</h2><p>{bid.port_name} · {bid.delivery_window}</p><p>{bid.effective_status} · deadline: {date(bid.deadline_at)} · revision {bid.revision}</p><section className="detail-section"><h3>Edit bid</h3>{detail === null ? <p>Loading bid detail…</p> : detail.quotes.length ? <p className="notice">The first quote freezes commercial terms. Only deadline changes remain available.</p> : <p>Commercial fields are editable until the first quote is retained.</p>}<label>Vessel / voyage<input aria-label="Edit vessel / voyage" disabled={!commercialOpen || pending} value={draft.vessel} onChange={(event) => setDraft({ ...draft, vessel: event.target.value })} /></label><label>Port<input aria-label="Edit port" disabled={!commercialOpen || pending} value={draft.port} onChange={(event) => setDraft({ ...draft, port: event.target.value })} /></label><label>Delivery window<input aria-label="Edit delivery window" disabled={!commercialOpen || pending} value={draft.window} onChange={(event) => setDraft({ ...draft, window: event.target.value })} /></label><label>Deadline<input aria-label="Edit deadline" type="datetime-local" disabled={!deadlineOpen || detail === null || pending} value={draft.deadline} onChange={(event) => setDraft({ ...draft, deadline: event.target.value })} /></label><FuelRows rows={draft.rows} onChange={(rows) => setDraft({ ...draft, rows })} disabled={!commercialOpen || pending} /><button type="button" disabled={!deadlineOpen || detail === null || !validRows || pending} onClick={saveBid}>Save bid</button></section><section className="detail-section"><h3>Responsibility</h3><select aria-label="Assign responsible BUYER" disabled={pending || bid.raw_status === 'awarded' || bid.raw_status === 'cancelled'} value={responsible} onChange={(event) => setResponsible(event.target.value)}>{buyers.map((buyer) => <option key={buyer.user_id} value={buyer.user_id}>{buyer.display_label}</option>)}</select><button type="button" disabled={pending || responsible === bid.responsible_buyer_user_id} onClick={() => void mutate(() => client.reassignBid(membershipId, bid.id, bid.revision, responsible))}>Reassign</button></section><section className="detail-section"><h3>Lifecycle</h3>{bid.raw_status === 'open' ? <button type="button" disabled={pending} onClick={() => void mutate(() => client.closeBid(membershipId, bid.id, bid.revision))}>Close</button> : null}{(bid.raw_status === 'closed' || (bid.raw_status === 'open' && bid.effective_status === 'closed')) ? <><label>Reopen deadline<input aria-label="Reopen deadline" type="datetime-local" value={draft.deadline} onChange={(event) => setDraft({ ...draft, deadline: event.target.value })} /></label><button type="button" disabled={pending} onClick={() => void mutate(() => client.reopenBid(membershipId, bid.id, bid.revision, localInputToIso(draft.deadline)))}>Reopen</button></> : null}{(bid.raw_status === 'open' || bid.raw_status === 'closed') ? cancelConfirm ? <button type="button" disabled={pending} onClick={() => void mutate(() => client.cancelBid(membershipId, bid.id, bid.revision))}>Confirm cancel</button> : <button type="button" className="secondary" disabled={pending} onClick={() => setCancelConfirm(true)}>Cancel bid</button> : null}</section><section className="detail-section"><h3>TRADER scope</h3>{bid.effective_status === 'open' ? <><select aria-label="Grant TRADER organization" value={scope} onChange={(event) => setScope(event.target.value)}><option value="">Select active TRADER organization</option>{organizations.filter((organization) => !detail?.access.some((access) => access.trader_organization_id === organization.organization_id)).map((organization) => <option key={organization.organization_id} value={organization.organization_id}>{organization.organization_label}</option>)}</select><button type="button" disabled={!scope || pending || detail === null} onClick={() => void mutate(() => client.grantBidTraderAccess(membershipId, bid.id, bid.revision, scope))}>Grant scope</button></> : null}<ul>{detail?.access.map((access) => <li key={access.trader_organization_id}>{access.trader_organization_label} <button type="button" className="secondary" disabled={pending} onClick={() => void mutate(() => client.revokeBidTraderAccess(membershipId, bid.id, bid.revision, access.trader_organization_id))}>Revoke</button></li>)}</ul></section><section className="detail-section"><h3>Buyer-visible quotes</h3>{detail?.quotes.map((quote) => <p key={quote.id}><strong>{quote.trader_organization_label}</strong> — total {number(quote.total_amount)} {quote.is_awarded ? ' (awarded)' : null}{quote.eligible_for_award && !quote.is_awarded ? awardConfirm === quote.id ? <button type="button" disabled={pending} onClick={() => void mutate(() => client.awardBid(membershipId, bid.id, bid.revision, quote.id, quote.revision))}>Confirm award</button> : <button type="button" disabled={pending} onClick={() => setAwardConfirm(quote.id)}>Award</button> : null}</p>)}</section><section className="detail-section"><h3>Audit history</h3><ol className="audit-list">{detail?.audit.map((event) => <li key={event.id}>{event.event_type} · revision {event.prior_revision ?? '—'} → {event.resulting_revision}</li>)}</ol></section><button type="button" className="secondary" onClick={refresh}>Refresh detail</button></div>;
}
