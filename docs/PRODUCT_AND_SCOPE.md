# Product And Scope

## Goal

Rebuild the SKRHAL bunker bidding system on a Supabase-backed stack while preserving the existing Firebase prototype only as a reference artifact.

## Implemented baseline

- The seller quote response migration is applied in Production as `20260901063926 seller_quote_response_model`. The repository has two later unapplied migrations, ending `20260902060817_unify_manual_bid_publish.sql`. Retained `awaiting`, `quoted`, and `gave_up` response states are commercial history only: explicit BID scope and active organization status remain the authorization authority.

- The PR #1 application foundation and preserved legacy reference are complete.
- Local Supabase migrations, pgTAP testing, and a CI database-validation job are implemented.
- Private accounts, organizations, memberships, and server-verified current-access context are implemented. The repository RPC exposes a trimmed organization label sourced from the active organization row.
- A sign-in-only frontend boundary exposes a minimal shell only after the server returns active membership context.
- The integrated RPC-only BUYER/TRADER workspace is implemented, including BUYER bid lifecycle and responsibility reassignment, explicit TRADER organization scope, retained organization response state, revision locking, terminal award, and append-only audit history. A response is `awaiting`, `quoted`, or `gave_up`; it is commercial history rather than authorization.
- BUYER-admin-only SELLER master management is implemented. SELLER is presentation terminology for the existing TRADER organization model. The backend migration is applied in Production as `20260828005713_trader_organization_admin`, and the BUYER-admin frontend code is merged and deployed through the existing frontend deployment path. The surface lists active/inactive/suspended organizations and active TRADER-membership counts, creates an organization only, and offers target-confirmed deactivation without delete or reactivation. Production read/load smoke and real SELLER mutations remain separate follow-up verification; no real SELLER add/deactivate smoke is claimed.
- The BUYER main area is a responsive operational bid board. Each card combines the bid summary with the union of current explicit BID scope and retained responses/quotes returned by the narrow BUYER-only SELLER-comparison RPC. `Awaiting quote`, `Quoted`, and `Gave up` remain distinct: gave-up rows retain history but display dashes, never rank, and cannot be awarded. Current access and organization activity remain independent metadata and authorization gates.
- PR #44's all-active-SELLER creation snapshot remains the currently applied Production behavior. The repository supersedes it for new manual and Mail Intake Publish: the BUYER form defaults all active SELLERs on but permits explicit toggles, and the server requires at least one selected active SELLER plus a future deadline. Only selected organizations receive access and awaiting responses in the authoritative creation transaction; no historical BID is backfilled.
- Fifteen migrations are applied in Production, ending with `20260901063926 seller_quote_response_model`; seventeen migrations exist in the repository. `20260902030228_mail_intake_prepared_bid_publish.sql` and `20260902060817_unify_manual_bid_publish.sql` remain repository-only. Production verification of the seller response migration confirmed that its backfill preserved existing business and history counts; a retained quote after GIVE UP is excluded from current comparison, and identical retained values can be resumed without a new quote revision.
- The PR #44 frontend code is merged, and the merged-main Vercel status reported a successful Production deployment. Its `Awaiting quote` server contract passed backend smoke, but no direct browser-authenticated Production UI smoke of the rendered row is claimed. The Production private Broadcast authorization foundation remains applied; Realtime service is enabled with public channel access disabled, enforcing private channels. Controlled BUYER/TRADER provisioning is complete, and the earlier Production UI smoke confirmed each role displays its trusted server organization label and enters its authorized workspace.
- The canonical Vercel Production deployment exists, and sanitized lifecycle smoke testing is complete using retained synthetic smoke records. Frontend private Realtime Production E2E verification is complete: a BUYER automatically reloaded after synthetic-bid creation; an authorized selected TRADER automatically reloaded after scope grant; a non-scoped TRADER and competitor organization remained isolated; and controlled `access_changed` removed protected workspace and failed closed after membership deactivation. Manual Refresh and post-mutation authoritative reload remained durable fallbacks, and controlled access state was restored after the smoke.
- Existing Auth users can request a self-service password reset. The response is non-enumerating, and a recovery session can only update a password before local sign-out and a new normal sign-in.
- The BUYER Publish new BID form accepts one manually selected local `.msg` up to 5 MiB, parses it in the browser into a review-only draft, shows warnings, and requires explicit Apply before populating supported visible fields. The human can edit every applied value, provide the required deadline, toggle the default-selected active SELLERs, and must explicitly choose `Publish BID`.
- The shared parser accepts existing operational body labels with an optional simple `-`, `*`, or `•` bullet and explicit `VSL`/`VESSEL` voyage fields. It conservatively falls back to a final `(VESSEL / DATE / PORT)` subject summary only when all three slash-separated parts are non-empty and the date includes a recognized English month plus a four-digit year. Explicit body fields and the existing clear English `BUNKER REQUEST` subject parsing retain precedence. Supported-grade description/specification rows without an MT/M/T quantity marker do not produce false invalid-quantity warnings. Valid supported-fuel quantity ranges separated by hyphen, ASCII tilde, en dash, em dash, or full-width tilde use the exact lower/left bound as the review candidate and produce a lower-bound verification warning, not an import-failure warning. Invalid, non-positive, malformed, and descending ranges remain rejected. These outputs remain review-only candidates and never infer a deadline or create a bid.
- Browser-local `.msg` parsing and the Gmail connector import the same shared parser. Production `gmail-mail-intake` v18 is ACTIVE, and controlled quantity-range smoke verified `40-50 -> 40` and `100~150 -> 100`; the synthetic queue row was dismissed normally. Existing pending queue rows were not reparsed because raw bodies are not retained, duplicate ingest does not overwrite candidates, and no cursor reset or historical scan occurred.
- A provider-neutral server-side intake foundation is active for operational mailbox intake: a private bounded normalized queue, service-role-only idempotent ingress, and active-BUYER-only pending-list and revision-locked dismiss RPCs. The BUYER frontend exposes only the shared pending list, manual Refresh, and target-confirmed irreversible dismiss; queue failures remain isolated from normal bid work. TRADERs have no access.
- The Production Gmail Edge Function is deployed and active for one controlled mailbox. It uses server-only IMAP with an App Password at fixed TLS `imap.gmail.com:993`, opens `INBOX` read-only, and has no OAuth/Gmail API or OAuth Testing-token dependency. Controlled Gmail/App Password setup, live mailbox connectivity, and initial high-water cursor initialization are complete with zero historical ingestion; later runs use a bounded UID snapshot and permit body analysis, the existing advisory parser, and ingest only when the original envelope subject begins at its first character with the exact case-sensitive ASCII prefix `//SPOT//`. The normalized stored subject retains the marker; parser subject input removes that leading marker and following whitespace. Filtered messages remain discovered without being absent or errors and may pass under the existing single successful cursor CAS. Eligible mail still analyzes only inline plain text up to 256 KiB total. UIDVALIDITY change fails closed without reset or historical rescan.
- PR #47's Production-applied `20260831071010_bid_operational_date.sql` adds one immutable server-derived Seoul operational date per BID. BUYERs choose a required date for all/my/responsible views and see pending mail whose authoritative `received_at` maps to that Seoul date. Historical selection disables new-BID creation in the UI. SELLER/TRADER users have no date selector: server RPCs return only current-Seoul-date BIDs and own quotes, and quote create/update reject non-current dates even when scope, organization, deadline, and effective-open checks otherwise pass. Historical data remains retained. The authenticated BUYER Production browser smoke created one `PR47 PROD SMOKE` BID on `2026-08-31`, which snapshot all five active SELLER organizations and one matching created audit; it was then normally cancelled and retained with its five scopes and created/cancelled audit history. Authenticated WORLD FUEL Production browser verification subsequently covered current-date submit, update, GIVE UP, resume, and a changed-price update under the seller response model.
- Production polling is active every five minutes through `pg_cron` to `pg_net` to the Edge Function. Its runtime project URL and connector trigger secret come from Supabase Vault entries named `gmail_connector_project_url` and `gmail_connector_trigger_secret`; no secret value or Production URL is stored in the repository migration.
- The deployed operational parser compatibility completed a sanitized Production post-deploy smoke through that existing scheduled polling path. One exact-marker operational mail produced normalized vessel, port, delivery-window, and supported-fuel quantity candidates without the prior false supported-grade invalid-quantity warnings; it remained a review-only pending queue candidate, not a bidding record, and created no bid. Existing older pending rows were not reparsed, and no cursor reset or historical rescan was performed.
- Production privileges were verified after applying the mail-intake and cursor migrations: the Supabase service role is ingest-only with no list/dismiss or direct queue CRUD, `authenticated` has list/dismiss only with no ingest or direct queue CRUD, and `anon` has neither RPC nor direct queue access. Manual connector E2E, exact `//SPOT//` eligibility re-smoke, and automatic polling smoke verification are complete. Synthetic Gmail smoke rows were cleaned afterward; operational intake may retain bounded normalized mail candidates.

## Excluded from this phase

- public signup, invitation, Auth-account or membership provisioning flows, SELLER rename/reactivation, and generic organization-status administration
- quote withdrawal, unaward, or award replacement
- historical `.msg`/`.eml` migration
- manual `.eml` intake; Microsoft Graph integration; webhook delivery; automatic cursor reset or historical import; and automatic bid creation
- approval and submission business rules
- migration or use of real operational bidding data

## Implemented business contracts

These contracts are implemented in the private database/RPC backend and surfaced through an integrated RPC-only frontend workspace:

- Approved BUYER accounts may number three or more.
- Every approved BUYER can see all bids and all quotes.
- BUYER views support all bids, bids created by the current BUYER, and filtering by BUYER.
- TRADER access requires verified membership in an active organization and an explicit current per-bid organization grant. Quotes are organization-owned, so active same-organization TRADER users may collaborate while competitors cannot see them.
- Authorization never trusts `user_metadata` or unverified client claims, and suspended or inactive users immediately lose access.
- `created_by` is immutable and remains distinct from the active BUYER actor who updates, closes, reopens, or cancels a record. Cross-BUYER changes preserve it and audit the actual actor.
- All active BUYERs can view and mutate bids. Responsibility is filtering only, not authorization; reassignment is permitted for raw open or closed bids, never cancelled bids.
- Raw states are `open`, `closed`, `awarded`, and `cancelled`; raw open becomes effectively closed when the server-time deadline passes. Quote creation/update is rejected after effective closure. Reopen preserves quotes and requires a null or future deadline.
- First quote freezes commercial bid terms and quantities; only a real deadline-only change is permitted while open. Cancellation and award are irreversible in V1. Award is an atomic eligible-quote selection; revocation is an immediate security operation and retains BUYER visibility of the quote.
- Browser code uses only the Supabase publishable key.
- Secret values and backend credentials never enter browser code, Vite variables, repository source values, logs, or test output; only server-side runtime variable names are present.
- The frontend gate mirrors server access for UX but does not replace RLS or server-side authorization.
- Local message parsing is advisory UX only. It never imports a deadline, SELLER authority, or responsible BUYER, never calls a server action, and cannot bypass the authoritative selected-scope `createBid` Publish RPC.
- Server-side mail ingress accepts only normalized bounded values and is executable only by the Supabase service role; browser, anonymous, and authenticated callers cannot invoke it or directly access the private queue. The exact opaque provider/mailbox/message identity is a database-enforced, non-overwriting idempotency key and is excluded from BUYER results.
- The mail intake queue deliberately retains no raw body, HTML, attachments, sender/recipient/address data, provider credentials, deadline, responsible BUYER, TRADER, seller, quote, award, or generic raw payload. Dismissal is one-way, row-locked, expected-revision checked, and records the actual authenticated user plus verified membership.
- The Gmail trigger is POST-only and authenticates its dedicated header before IMAP, database, or cursor work. Browser JWTs are not connector authority. UIDVALIDITY change, any IMAP/message retrieval/processing failure other than a definitive snapshot UID absence, or stale cursor revision fails closed without automatic reset, historical scan, or cursor advance. A definitive missing message is counted only in a bounded aggregate and is not ingested; after all remaining messages succeed, the cursor may advance. Ingest serialization failures retry at most three attempts.
- Gmail intake retains no raw body, MIME, HTML, attachments, sender/recipient/address data, or credentials and does not perform an automatic historical import. It creates bounded normalized candidates only. A current active BUYER may open one as a private prepared BID form, review/edit the parsed values and warnings, select active SELLER participants, enter a deadline, and explicitly Publish. Only that server-authorized Publish converts the intake once and creates the authoritative BID; Gmail never auto-publishes. Converted items leave the pending list, and TRADERs never see prepared intake data.
- An approved BUYER may take over or modify another BUYER's bid only through server-authorized policy. `created_by` remains immutable, and the actual actor plus any responsible-BUYER reassignment are retained in audit history.
- Only a current active server-verified `buyer_admin` may use SELLER administration RPCs. `buyer_operator` retains normal bidding authority but has no SELLER list/create/deactivate authority. TRADER, anonymous, inactive, suspended, cross-user, and client-claimed roles are denied.
- SELLER creation trims a required name, limits it to 120 characters, and creates exactly one active TRADER organization. It creates no login, invitation, Auth user, membership, BID scope, quote, or bid. Case-insensitive surrounding-whitespace-normalized TRADER names are database-unique across every status; BUYER organization names are outside that rule.
- SELLER deactivation locks the target and changes active to inactive. It never deletes the organization, memberships, retained BID scopes, quotes, awards, or audit history. Inactive is idempotent; suspended is rejected; no reactivation or generic status-update operation exists. Any future reactivation must explicitly decide whether retained old BID scopes may revive authority.
- SELLER creation and actual deactivation each append one private immutable admin audit event with server-verified actor identity and bounded before/after organization identity/status snapshots. Realtime carries only existing invalidation markers: creation calls BUYER workspace invalidation, and deactivation uses the existing organization-status trigger for member access and BUYER refresh.

The frontend selects only server-returned memberships, never grants authority, clears protected data on context switch, sign-out, password recovery, or authorization failure, and uses manual refresh plus post-mutation server reload. It consumes private Realtime Broadcast only as best-effort invalidation: one self access topic and one selected-context business topic cause server access revalidation or an authoritative BUYER/TRADER RPC reload, never payload rendering. Membership selectors and chips present the server label as display data only; the old four-field RPC shape uses a shortened neutral organization ID solely for safe frontend-first rollout ordering. The server-returned BUYER role controls only whether SELLER management is presented; every operation is re-authorized by PostgreSQL. Password recovery is limited to existing Auth users and does not grant workspace access. Invitations, account/membership provisioning, SELLER rename/reactivation, and generic administration remain unimplemented. No real operational bidding data has been migrated or is in use; retained Production records are synthetic smoke records only.
